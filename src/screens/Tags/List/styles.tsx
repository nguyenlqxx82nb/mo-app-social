import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {

	},
	scrollView: {
		flexDirection: 'row',
		flexWrap: 'wrap',
		maxHeight: 35,
		overflow: 'hidden'
	}
});

export default styles;

